/* 
	Sets up what the ERA type does. Backend to ERAS
*/

#include <stdio.h>
#include "types.h"

// Gathers the Era name using the DefaultMainDbNode struct
void *
GatherEra(char *Era, DefaultMainDbNode *Eras/*Used to go through all ERAS setup through the Default Node*/) {

	for(int i = 0; i < 4; i++) {}
	return 0;
}
